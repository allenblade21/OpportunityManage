# 机汇 OpportunityManage · 商机管理系统

一个面向个人销售 / 小型销售团队的轻量商机管理工具(Web 版):以**商机**为中心,把**联系人、跟进项、想法**有序地组织起来,统一的 P0–P3 优先级贯穿全局,保证每个商机永远有"下一步"。

> 当前为 **v0.1 MVP**:设计文档齐备,应用已完成初步开发(本地单机版,数据存于浏览器 localStorage)。

## 仓库内容

| 路径 | 说明 |
| --- | --- |
| [`docs/design.md`](docs/design.md) | 产品设计方案:定位、数据模型、优先级体系、信息架构、技术选型与迭代路线 |
| [`docs/workflows.md`](docs/workflows.md) | 核心流转与流程图:商机阶段状态机、跟进闭环、想法流转、页面导航、数据联动规则 |
| [`docs/ui-design.md`](docs/ui-design.md) | UI 设计规范:Design Tokens、字体排版、组件规范、页面规格、响应式与深色模式 |
| [`prototype/index.html`](prototype/index.html) | 可交互高保真 UI 原型(单文件,浏览器直接打开) |
| [`src/`](src/) | MVP 应用源码(React 18 + TypeScript + Vite + zustand) |

## 运行应用(MVP)

```bash
npm install
npm run dev       # 开发模式,默认 http://localhost:5173
npm run build     # 类型检查 + 生产构建(输出 dist/)
npm run preview   # 预览生产构建
```

**已实现:** 四对象(商机/联系人/跟进项/想法)创建与流转、商机看板/列表/详情、阶段步进推进(自动写时间线与默认赢率)、赢单/输单(必填原因)、跟进完成闭环(引导创建下一次跟进、刷新联系人最近联系时间)、想法转跟进/升级商机、工作台实时聚合(KPI、漏斗、逾期与「无下一步」警示)、全局快速新建、深色模式、响应式、localStorage 持久化(含演示数据,可在侧栏重置)。

**规划中(见 [迭代路线](docs/design.md#9-迭代路线)):** 看板拖拽、全局搜索、编辑表单、日历视图与提醒、多用户后端。

## 查看原型

```bash
# 任选其一
open prototype/index.html            # macOS
xdg-open prototype/index.html        # Linux
# 或直接把文件拖进浏览器
```

原型为纯静态页面(无任何依赖),支持:页面切换、看板/列表视图切换、商机详情、勾选完成跟进、快速新建弹窗、深色模式(跟随系统)与窄屏响应式。

## 核心设计一览

- **四个核心对象:** 商机(阶段推进)· 联系人(角色分类)· 跟进项(时间分组 + 闭环)· 想法(可转化为跟进/商机)
- **统一优先级:** P0 紧急(红)/ P1 重要(橙)/ P2 普通(蓝)/ P3 观察(灰),全局排序过滤
- **阶段流:** 线索 → 初步接触 → 需求确认 → 方案报价 → 谈判 → 赢单/输单,色带由浅至深表达"离成交越近"
- **跟进闭环:** 完成跟进 → 填写结果 → 引导创建下一次跟进,杜绝商机被"晾着"

详细内容见 [设计方案文档](docs/design.md)。
